/**
 * 全项目规则常量 —— 唯一来源。
 *
 * 约束（架构差异报告 M7 / 确认稿）：
 *   - Prompt、Zod Schema、规则校验、UI 文案都必须从这里取值，
 *     任何地方都不允许再出现 20 / 5 / 1200 这类硬编码字面量。
 *   - 本文件是纯常量模块，禁止 import 任何 AI、网络或 React 代码。
 */

/* ------------------------------------------------------------------ *
 * 标题（规格书 §9.1 / §9.3）
 * ------------------------------------------------------------------ */

/**
 * 标题字符数上限。
 *
 * 注意：这是本项目当前采用的校验口径。抖音实际发布端的字符计算方式
 * （英文、数字、emoji 是否折算）需要后续通过官方文档与真实设备验证；
 * 由于计数口径只在规则层单点实现，口径变更时只需改这里 + 跑一遍单测。
 */
export const TITLE_MAX = 20;

/** 标题最少字符数（防止空标题或纯标点）。 */
export const TITLE_MIN = 1;

/**
 * 生成标题时给模型的内部目标字数。
 *
 * 比 TITLE_MAX 少 2 字，留出缓冲以降低越界率；
 * 它只影响 Prompt，不参与最终校验（最终一律以 TITLE_MAX 判定）。
 */
export const TITLE_TARGET = 18;

/** 每次生成/展示的标题数量（规格书 §8）。 */
export const TITLE_COUNT = 5;

/* ------------------------------------------------------------------ *
 * 正文（规格书 §10）
 * ------------------------------------------------------------------ */

export const BODY_MIN = 20;
export const BODY_MAX = 1200;

/* ------------------------------------------------------------------ *
 * 标签（规格书 §11 + 确认稿 M8/M9）
 * ------------------------------------------------------------------ */

/** 最终保留的标签数量上限。 */
export const TAG_MAX = 5;

/** 单个标签的字符数上限，超出按非法标签丢弃。 */
export const TAG_MAX_LENGTH = 20;

/** 输出给用户与复制到剪贴板的统一前缀，保证复制后可直接成为抖音话题。 */
export const TAG_PREFIX = "#";

/* ------------------------------------------------------------------ *
 * 选题（规格书 §7）
 * ------------------------------------------------------------------ */

/** AI 必须给出的选题数量。 */
export const TOPIC_COUNT = 3;

/* ------------------------------------------------------------------ *
 * 图片（规格书 §5 + 架构差异报告 M14）
 * ------------------------------------------------------------------ */

export const IMAGE_MIN = 1;
export const IMAGE_MAX = 9;

/** 允许上传的图片 MIME 类型。 */
export const IMAGE_ACCEPTED_MIME_TYPES = [
  "image/jpeg",
  "image/png",
  "image/webp",
] as const;

/** 允许上传的图片扩展名，用于兜底校验 MIME 缺失的情况。 */
export const IMAGE_ACCEPTED_EXTENSIONS = [".jpg", ".jpeg", ".png", ".webp"] as const;

/** 客户端压缩时长边上限（像素）。压缩既降低视觉 Token 成本，也规避请求体上限。 */
export const IMAGE_MAX_EDGE = 1568;

/** 客户端压缩后的 JPEG 质量。 */
export const IMAGE_JPEG_QUALITY = 0.8;

/** 单张图片压缩后的字节上限（2MB），超出则拒绝并提示。 */
export const IMAGE_MAX_BYTES = 2 * 1024 * 1024;

/** 服务端单次请求体字节上限（4MB），与 IMAGE_MAX_BYTES 一起构成双层防线。 */
export const UPLOAD_MAX_BYTES = 4 * 1024 * 1024;

/* ------------------------------------------------------------------ *
 * Brief（跨步骤复用的紧凑上下文，规格书 §6 / §25）
 * ------------------------------------------------------------------ */

/**
 * Brief 的字符数上限。
 * 服务端对前端回传的 brief 做长度校验，避免被构造成超长文本放大 Token 消耗。
 */
export const BRIEF_MAX = 300;
